stages:
- validation

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "parent_pipeline"

default:
  before_script:
    - python3 --version
    - pip3 --version

validateDependencies-openjdk11:
  tags:
  - openjdk11
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-openjdk21:
  tags:
  - openjdk21
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-node10:
  tags:
  - node10
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-flutter:
  tags:
  - flutter
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-dotnet6:
  tags:
  - dotnet6
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-dotnet8:
  tags:
  - dotnet8
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-go:
  tags:
  - go
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-frontend:
  tags:
  - frontend
  stage: validation
  script:
  - pip3 install -r requirements.txt

validateDependencies-iac:
  tags:
  - iac
  stage: validation
  script:
  - pip3 install -r requirements.txt --break-system-packages
