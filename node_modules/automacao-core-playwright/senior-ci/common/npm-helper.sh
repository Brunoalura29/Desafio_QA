install() {
  if [[ ! -f "package-lock.json" ]]
  then
    npm install  
  fi

  npm ci --cache .npm --prefer-offline
}

get_attribute() {
    cat package.json | grep $1 | head -1 | cut -d ":" -f 2 | sed "s/[{, }, \", ',[:space:]]//g";
}
